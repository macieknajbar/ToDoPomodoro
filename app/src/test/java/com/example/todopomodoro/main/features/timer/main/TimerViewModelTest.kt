package com.example.todopomodoro.main.features.timer.main

import org.junit.Assert.*
import org.junit.Test

class TimerViewModelTest {

    @Test
    fun `ON onStartClicked SHOULD update state`() {
        assertEquals(
            sut.state.value.
        )
    }
}